@model IEnumerable<AppTemplateDAL.Models.Master.StationsBO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.citycountry.cityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StationName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.citycountry.cityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StationName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StationID })
        </td>
    </tr>
}

</table>
